This prev directory consists of previous version of files tested for the model
The repo might be more cleaner if we find a way to
take log of changes made through out the model
including idea, algorithm, parameter and so on
if anyone has a opinion talk with @sleepylee02
